let promoCode = prompt("Введите промокод:");

// Приводим введённый промокод к нижнему регистру и проверяем его
if (promoCode === "СкиДкА") {
  alert("Промокод применён");
} else {
  alert("Промокод не работает");
}
